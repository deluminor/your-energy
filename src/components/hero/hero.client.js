/**
 * Client entry for the hero island. Owns the hero search field — normalize the
 * query (utils/validators), push it to the store (services/store), and scroll
 * to the exercises section. See `quote/quote.client.js` for the reference pattern.
 *
 * @param {HTMLElement | null} root
 * @returns {void}
 */
export function initHero(root) {
  if (!root) return;
}
