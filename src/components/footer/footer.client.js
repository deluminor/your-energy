/**
 * Client entry for the footer island. Owns the newsletter form — validate the
 * email (utils/validators), submit it (api/subscription), and report the result
 * via toasts (utils/notify). See `quote/quote.client.js` for the reference pattern.
 *
 * @param {HTMLElement | null} root
 * @returns {void}
 */
export function initFooter(root) {
  if (!root) return;
}
