/**
 * Client entry for the header island. Owns navigation behavior — mobile menu
 * toggle and active-link highlighting. Import api/services/utils here when
 * implementing; see `quote/quote.client.js` for the reference pattern.
 *
 * @param {HTMLElement | null} root
 * @returns {void}
 */
export function initHeader(root) {
  if (!root) return;
}
